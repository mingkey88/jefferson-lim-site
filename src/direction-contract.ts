/**
 * The Impeccable direction contract for this build.
 *
 * Rendered as an HTML comment in the document body during development so it
 * stays auditable while editing, and stripped from production output — this is
 * a regulated client's public site, not a place for internal build rationale.
 */
export const CONTRACT = `<!--
  THESIS: A Singapore financial consultant's practice rendered as engraved
  security printing — the bond certificate, not the insurance brochure. It
  refuses the category default of stock-photo bands stacked down a builder
  column, and refuses the icon-card grid as page structure.

  OWN-WORLD: Deep navy stock (#061223 to #11304e), engraver's gold (#c9a55c)
  for every heading and rule, warm paper (#f5f2ec) for wordings. Bodoni Moda
  didone display against Archivo text. Structure is carried by gold hairlines
  and lozenge-broken rules, not boxes. Real hypotrochoid guilloché rosettes,
  generated as geometry, sit behind the opening and the seal.

  STORY: A visitor arriving from a name card or referral learns Jefferson spent
  his career inside finance — audit, bonds, and finally building a public
  hospital's finance function — then sees the five planning pillars and starts a
  conversation. Trust is earned by specificity, not adjectives.

  FIRST VIEWPORT: Full-height navy field. A slowly turning guilloché rosette
  bleeds off the right. Left, at ~5rem, a gold didone position statement —
  planning is for everyone, not just the wealthy — broken at the comma across
  three lines. Beneath it a lozenge rule, then the lede at 68ch which is where
  Jefferson introduces himself by name, then the gold pill primary action and an
  outlined secondary. Amended at finish: the contract originally promised the
  name in display type; the claim leads instead and the lede carries the name.

  FORM: Brief-pinned direction — the client's written spec fixes the palette
  (dark blue ground, golden headings, white wordings), the pill navigation and
  the four page labels. No concept roll was run; a pinned brief beats the roll.
  Rendition (engraved security printing) is derived, not pinned.

  FINISH: unreviewed and undocumented is unfinished; this build ends with the
  finish review, the verdict, and DESIGN.md
-->`;
